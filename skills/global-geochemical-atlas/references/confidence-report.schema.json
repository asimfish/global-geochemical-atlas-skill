{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:ai4s:geochemistry-confidence-report:d2-v3",
  "title": "D2 v3 operational confidence report",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "confidence_version",
    "name",
    "not_a_probability",
    "meaning",
    "run_metadata",
    "weights",
    "component_definitions",
    "source_scoring",
    "band_thresholds",
    "gates",
    "component_means",
    "band_counts",
    "gate_counts"
  ],
  "properties": {
    "confidence_version": {"const": "d2-confidence-v3"},
    "name": {"const": "operational_confidence"},
    "not_a_probability": {"const": true},
    "meaning": {"type": "string", "minLength": 1},
    "run_metadata": {
      "type": "object",
      "required": ["run_id", "input_sha256", "input_row_count", "configuration"],
      "properties": {
        "run_id": {"type": "string", "minLength": 1},
        "input_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
        "input_row_count": {"type": "integer", "minimum": 1},
        "configuration": {"type": "object"}
      },
      "additionalProperties": true
    },
    "weights": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "completeness", "method", "spatial", "qc"],
      "properties": {
        "source": {"type": "number", "minimum": 0, "maximum": 1},
        "completeness": {"type": "number", "minimum": 0, "maximum": 1},
        "method": {"type": "number", "minimum": 0, "maximum": 1},
        "spatial": {"type": "number", "minimum": 0, "maximum": 1},
        "qc": {"type": "number", "minimum": 0, "maximum": 1}
      }
    },
    "component_definitions": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "completeness", "method", "spatial", "qc", "overall"],
      "properties": {
        "source": {"type": "string", "minLength": 1},
        "completeness": {"type": "string", "minLength": 1},
        "method": {"type": "string", "minLength": 1},
        "spatial": {"type": "string", "minLength": 1},
        "qc": {"type": "string", "minLength": 1},
        "overall": {"type": "string", "minLength": 1}
      }
    },
    "source_scoring": {
      "type": "object",
      "additionalProperties": false,
      "required": ["tier_scores", "penalties", "evidence_boundary"],
      "properties": {
        "tier_scores": {
          "type": "object",
          "additionalProperties": {"type": "number", "minimum": 0, "maximum": 1}
        },
        "penalties": {
          "type": "object",
          "additionalProperties": {"type": "number", "minimum": 0, "maximum": 1}
        },
        "evidence_boundary": {"type": "string", "minLength": 1}
      }
    },
    "band_thresholds": {
      "type": "object",
      "additionalProperties": false,
      "required": ["high", "medium", "low"],
      "properties": {
        "high": {"type": "string"},
        "medium": {"type": "string"},
        "low": {"type": "string"}
      }
    },
    "gates": {"type": "object", "additionalProperties": {"type": "string"}},
    "gate_counts": {
      "type": "object",
      "additionalProperties": {"type": "integer", "minimum": 0}
    },
    "component_means": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "completeness", "method", "spatial", "qc", "overall"],
      "properties": {
        "source": {"type": "number", "minimum": 0, "maximum": 1},
        "completeness": {"type": "number", "minimum": 0, "maximum": 1},
        "method": {"type": "number", "minimum": 0, "maximum": 1},
        "spatial": {"type": "number", "minimum": 0, "maximum": 1},
        "qc": {"type": "number", "minimum": 0, "maximum": 1},
        "overall": {"type": "number", "minimum": 0, "maximum": 1}
      }
    },
    "band_counts": {
      "type": "object",
      "additionalProperties": false,
      "required": ["high", "medium", "low"],
      "properties": {
        "high": {"type": "integer", "minimum": 0},
        "medium": {"type": "integer", "minimum": 0},
        "low": {"type": "integer", "minimum": 0}
      }
    }
  }
}
