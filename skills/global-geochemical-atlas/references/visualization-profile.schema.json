{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:ai4s:global-geochemical-atlas:d3-visualization-profile-v2",
  "title": "D3 visualization profile",
  "type": "object",
  "required": [
    "schema_version",
    "title",
    "subtitle",
    "story",
    "theme",
    "spatial_scope",
    "default_region",
    "custom_region",
    "filters",
    "comparison",
    "display"
  ],
  "properties": {
    "schema_version": {"const": "d3-visualization-profile-v2"},
    "title": {"type": "string", "minLength": 1, "maxLength": 120},
    "subtitle": {"type": "string", "minLength": 1, "maxLength": 500},
    "story": {
      "enum": ["overview", "coverage", "anomaly", "comparison", "database", "evidence"]
    },
    "theme": {"const": "evidence-dark"},
    "spatial_scope": {
      "enum": ["global", "regional"],
      "description": "global generates a world product; regional clips map payloads and GeoJSON to default_region."
    },
    "default_region": {
      "enum": ["global", "usa", "usa48", "china", "shanghai", "europe", "australia", "custom"]
    },
    "custom_region": {
      "oneOf": [
        {"type": "null"},
        {
          "type": "object",
          "required": ["label", "bounds"],
          "properties": {
            "label": {"type": "string", "minLength": 1, "maxLength": 80},
            "bounds": {
              "type": "object",
              "required": ["w", "s", "e", "n"],
              "properties": {
                "w": {"type": "number", "minimum": -180, "maximum": 180},
                "s": {"type": "number", "minimum": -90, "maximum": 90},
                "e": {"type": "number", "minimum": -180, "maximum": 180},
                "n": {"type": "number", "minimum": -90, "maximum": 90}
              },
              "additionalProperties": false
            },
            "country_code": {
              "type": ["string", "null"],
              "pattern": "^[A-Z]{3}$"
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "filters": {
      "type": "object",
      "required": ["element", "medium", "sample_type", "basis", "geology", "method", "method_scope", "source", "confidence"],
      "properties": {
        "element": {"type": ["string", "null"]},
        "medium": {"type": ["string", "null"]},
        "sample_type": {"type": ["string", "null"]},
        "basis": {"type": ["string", "null"]},
        "geology": {"type": ["string", "null"]},
        "method": {"type": ["string", "null"]},
        "method_scope": {"type": ["string", "null"]},
        "source": {"type": ["string", "null"]},
        "confidence": {"type": ["string", "null"]}
      },
      "additionalProperties": false
    },
    "comparison": {
      "type": "object",
      "required": ["x", "y", "medium"],
      "properties": {
        "x": {"type": ["string", "null"]},
        "y": {"type": ["string", "null"]},
        "medium": {"type": ["string", "null"]}
      },
      "additionalProperties": false
    },
    "display": {
      "type": "object",
      "required": ["map_mode", "color_by", "anomaly_grid_degrees", "show_anomaly_points", "show_anomaly_regions"],
      "properties": {
        "map_mode": {"enum": ["distribution", "heat", "combined"]},
        "color_by": {"enum": ["medium", "element", "value"]},
        "anomaly_grid_degrees": {"enum": [1, 2, 5]},
        "show_anomaly_points": {"type": "boolean"},
        "show_anomaly_regions": {"type": "boolean"}
      },
      "additionalProperties": false
    }
  },
  "allOf": [
    {
      "if": {"properties": {"spatial_scope": {"const": "global"}}},
      "then": {
        "properties": {
          "default_region": {"const": "global"},
          "custom_region": {"type": "null"}
        }
      }
    },
    {
      "if": {"properties": {"spatial_scope": {"const": "regional"}}},
      "then": {
        "properties": {
          "default_region": {
            "enum": ["usa", "usa48", "china", "shanghai", "europe", "australia", "custom"]
          }
        }
      }
    },
    {
      "if": {"properties": {"default_region": {"const": "custom"}}},
      "then": {"properties": {"custom_region": {"type": "object"}}},
      "else": {"properties": {"custom_region": {"type": "null"}}}
    },
    {
      "if": {"properties": {"story": {"const": "comparison"}}},
      "then": {
        "properties": {
          "comparison": {
            "properties": {
              "x": {"type": "string", "minLength": 1},
              "y": {"type": "string", "minLength": 1}
            }
          }
        }
      }
    }
  ],
  "additionalProperties": false
}
